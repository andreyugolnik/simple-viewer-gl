language: c

cache: true

os:
    - linux

install:
  - sudo apt-get -qq -y update
  - sudo apt-get install cmake
  - sudo apt-get install freetype2

compiler:
    - gcc
    - clang

build:
    ci:
        - make clean
        - make release
