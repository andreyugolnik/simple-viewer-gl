language: c

cache: true

os:
    - linux

install:
  - sudo apt-get -qq -y update
  - sudo apt-get install cmake freetype

compiler:
    - gcc
    - clang

build:
    ci:
        - make clean
        - make release
